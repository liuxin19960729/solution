## 设置环境变量
```
临时环境变量
  windows:
     set  xxx=xx;

  linux :
      xxx=xxx  or  export xxx=xx
       


永久环境变量
  在 Unix上 ， 典型的 shell 启动文件 是bash.bashrc或tcsh。 .bash_profile   .tcshrc
  
  ~/bash_profile

编译安装并且能够通过简单指定启用
PATH=${PATH}:/usr/local/mysql/bin

source xxxx.bash_profile 把新的配置加入到内存

```