## 传输的数据进行压缩
```
默认情况下连接数据未压缩

压缩 会造成客户端和服务器的CPU负载 (服务器 和 客户端 都会 发送数据(压缩)  接收数据(解压))

好处:网络带宽低(压缩后数据包变小),数据带宽延迟低


客户端 --compresss 指示压缩链接到服务器
对于源/副本复制，启用 slave_compressed_protocol 系统变量指定对源的副本连接使用压缩

必须双方都支持压缩 zlib ,否则会回退到未压缩的连接

```